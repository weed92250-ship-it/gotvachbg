let recipes=[];
const $=id=>document.getElementById(id),panel=$('panel'),loginBox=$('loginBox');
async function api(url,opt={}){opt.headers={...(opt.headers||{}),'content-type':'application/json'};const r=await fetch(url,opt);const d=await r.json();if(r.status===401){showLogin();throw Error('Сесията изтече')}return d}
function showLogin(){loginBox.hidden=false;panel.hidden=true}
async function load(){recipes=await api('/api/recipes');$('count').textContent='Общо рецепти: '+recipes.length;$('list').innerHTML=recipes.map(r=>'<article class="card"><div class="card-body"><span class="tag">'+r.category+'</span><h3>'+r.title+'</h3><p>'+r.excerpt+'</p><div class="meta"><span>'+r.date+'</span><span><button class="btn" data-edit="'+r.id+'">Редактирай</button> <button class="btn" data-delete="'+r.id+'">Изтрий</button></span></div></div></article>').join('');document.querySelectorAll('[data-edit]').forEach(b=>b.onclick=()=>edit(b.dataset.edit));document.querySelectorAll('[data-delete]').forEach(b=>b.onclick=()=>removeRecipe(b.dataset.delete))}
function edit(id){const r=recipes.find(x=>x.id===id);if(!r)return;$('id').value=r.id;$('title').value=r.title;$('category').value=r.category;$('area').value=r.area;$('image').value=r.image||'';$('excerpt').value=r.excerpt;$('ingredients').value=(r.ingredients||[]).map(x=>x.name+' | '+x.measure).join('\n');$('instructions').value=r.instructions;window.scrollTo({top:0,behavior:'smooth'})}
async function removeRecipe(id){if(!confirm('Да изтрия ли тази рецепта?'))return;await api('/api/recipes',{method:'DELETE',body:JSON.stringify({id})});load()}
$('login').onsubmit=async e=>{e.preventDefault();const res=await fetch('/api/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({password:$('password').value})});const d=await res.json();if(!res.ok){$('loginStatus').textContent=d.error||'Грешка';return}$('password').value='';panel.hidden=false;loginBox.hidden=true;load()}
$('recipeForm').onsubmit=async e=>{e.preventDefault();const ingredients=$('ingredients').value.split('\n').map(x=>{const [name,...rest]=x.split('|');return {name:name.trim(),measure:rest.join('|').trim()}}).filter(x=>x.name);const id=$('id').value;const body={title:$('title').value.trim(),category:$('category').value.trim(),area:$('area').value.trim(),image:$('image').value.trim(),excerpt:$('excerpt').value.trim(),ingredients,instructions:$('instructions').value.trim()};await api('/api/recipes',{method:id?'PUT':'POST',body:JSON.stringify(id?{...body,id}:body)});e.target.reset();$('id').value='';load()};
$('cancel').onclick=()=>{$('recipeForm').reset();$('id').value=''};
$('logout').onclick=async()=>{await fetch('/api/logout',{method:'POST'});showLogin()};
load().then(()=>{loginBox.hidden=true;panel.hidden=false}).catch(showLogin);
