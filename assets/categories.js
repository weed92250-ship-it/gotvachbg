const fallback='https://images.unsplash.com/photo-1546069901-ba9599a7e63c?w=1000&auto=format&fit=crop&q=85';
const grid=document.querySelector('#categoryGrid'), chips=document.querySelector('#categoryChips'), search=document.querySelector('#categorySearch'), status=document.querySelector('#categoryStatus');
let recipes=[],active='Всички';
const safe=s=>s&&s.startsWith('http')&&!s.includes('pollinations.ai')&&!s.includes('[https://')?s:fallback;
const card=r=>'<article class="card"><img loading="lazy" src="'+safe(r.img)+'" alt="'+r.title.replace(/"/g,'&quot;')+'"><div class="card-body"><span class="tag">'+(r.category||'Рецепта')+'</span><h3><a href="'+r.url+'">'+r.title+'</a></h3><p>'+ (r.excerpt||'Домашна рецепта с ясни стъпки.') +'</p><div class="meta"><span>⏱ '+(r.time||'')+'</span><span>🌍 '+(r.area||'Домашна')+'</span></div></div></article>';
function render(){const q=search.value.trim().toLocaleLowerCase('bg');const list=recipes.filter(r=>(active==='Всички'||r.category===active)&&(!q||[r.title,r.excerpt,r.category,r.area,r.ingredients].join(' ').toLocaleLowerCase('bg').includes(q)));status.textContent='Показани рецепти: '+list.length;grid.innerHTML=list.length?list.map(card).join(''):'<div class="empty">Няма рецепти за избраните условия.</div>'}
function setActive(c){active=c;document.querySelectorAll('[data-category]').forEach(x=>x.setAttribute('aria-current',x.dataset.category===c?'true':'false'));render()}
fetch('/recipes.json').then(r=>r.json()).then(d=>{recipes=Array.isArray(d)?d:[];const cats=['Всички',...new Set(recipes.map(r=>r.category).filter(Boolean))];chips.innerHTML=cats.map(c=>'<button class="chip" type="button" data-category="'+c.replace(/"/g,'&quot;')+'">'+c+'</button>').join('');document.querySelectorAll('[data-category]').forEach(b=>b.addEventListener('click',()=>setActive(b.dataset.category)));render()}).catch(()=>status.textContent='Рецептите временно не се зареждат.');
search.addEventListener('input',render);document.querySelector('#clearSearch').addEventListener('click',()=>{search.value='';setActive('Всички');});
