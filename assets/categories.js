const fallback='https://images.unsplash.com/photo-1546069901-ba9599a7e63c?w=1000&auto=format&fit=crop&q=85';
const grid=document.querySelector('#categoryGrid'),chips=document.querySelector('#categoryChips'),search=document.querySelector('#categorySearch'),status=document.querySelector('#categoryStatus');let recipes=[],active='Всички';
const safe=s=>s&&s.startsWith('http')&&!s.includes('pollinations.ai')&&!s.includes('[https://')?s:fallback;
const normalize=r=>({...r,img:r.img||r.image||fallback,url:r.url||('/recipe/'+r.id),excerpt:r.excerpt||'Домашна рецепта с ясни стъпки.',category:r.category||'Рецепта',area:r.area||'Домашна',ingredients:Array.isArray(r.ingredients)?r.ingredients:[]});
const card=r=>'<article class="card"><img loading="lazy" src="'+safe(r.img)+'" alt="'+String(r.title||'Рецепта').replace(/"/g,'&quot;')+'"><div class="card-body"><span class="tag">'+r.category+'</span><h3><a href="'+r.url+'">'+r.title+'</a></h3><p>'+r.excerpt+'</p><div class="meta"><span>⏱ '+(r.time||'')+'</span><span>🌍 '+r.area+'</span></div></div></article>';
function render(){const q=search.value.trim().toLocaleLowerCase('bg');const list=recipes.filter(r=>(active==='Всички'||r.category===active)&&(!q||[r.title,r.excerpt,r.category,r.area,...r.ingredients.map(x=>x.name||''),...r.ingredients.map(x=>x.measure||'')].join(' ').toLocaleLowerCase('bg').includes(q)));status.textContent='Показани рецепти: '+list.length;grid.innerHTML=list.length?list.map(card).join(''):'<div class="empty">Няма рецепти за избраните условия.</div>'}
function setActive(c){active=c;document.querySelectorAll('[data-category]').forEach(x=>x.setAttribute('aria-current',x.dataset.category===c?'true':'false'));render()}
async function loadRecipes(){try{const api=await fetch('/api/recipes',{cache:'no-store'});if(api.ok){const data=await api.json();if(Array.isArray(data)&&data.length)return data.map(normalize)}}catch(e){}return (await (await fetch('/recipes.json')).json()).map(normalize)}
loadRecipes().then(data=>{recipes=data;const cats=['Всички',...new Set(recipes.map(r=>r.category).filter(Boolean))];chips.innerHTML=cats.map(c=>'<button class="chip" type="button" data-category="'+c.replace(/"/g,'&quot;')+'">'+c+'</button>').join('');document.querySelectorAll('[data-category]').forEach(b=>b.addEventListener('click',()=>setActive(b.dataset.category)));render()}).catch(()=>status.textContent='Рецептите временно не се зареждат.');
search.addEventListener('input',render);
document.querySelector('#clearSearch').addEventListener('click',()=>{search.value='';setActive('Всички')});
